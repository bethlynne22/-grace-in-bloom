function go(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('show'));document.getElementById(id).classList.add('show');location.hash=id;setTimeout(resizeCanvases,100)}if(location.hash)go(location.hash.slice(1));const days=[["Day 1 — Light","God speaks light into darkness. Honey Drop: God brings light where there was none."],["Day 2 — Sky","God separates the waters and makes the sky. Honey Drop: God creates space for life to flourish."],["Day 3 — Land and Plants","Dry land appears, and plants begin to grow. Honey Drop: God fills the earth with beauty."],["Day 4 — Sun, Moon, and Stars","God sets lights in the sky. Honey Drop: God orders time with care."],["Day 5 — Birds and Fish","The waters and skies fill with living creatures. Honey Drop: Life overflows from God's command."],["Day 6 — Animals and Humanity","God makes animals and humans in His image. Honey Drop: You are created with sacred worth."],["Day 7 — Rest","God rests from His work. Honey Drop: Rest belongs in God's good design."]];const daysDiv=document.getElementById('days');days.forEach(d=>{let c=document.createElement('div');c.className='card';c.innerHTML=`<h2>${d[0]}</h2><p>${d[1]}</p><textarea id="${d[0].replaceAll(' ','_')}" placeholder="My notes on ${d[0]}"></textarea>`;daysDiv.appendChild(c)});document.getElementById('wordGrid').innerHTML=["GODLIGHTXX","WATERDAYXG","EARTHMOONX","NIGHTSTARS","SUNGOODXX","CREATEEEE"].map(r=>r.split('').join(' ')).join('<br>');let drawColor='#4f3d36',drawSize=4;function setColor(c){drawColor=c}function setSize(s){drawSize=s}function setupCanvas(id){const canvas=document.getElementById(id);if(!canvas)return;const ctx=canvas.getContext('2d');function resize(){const data=localStorage.getItem(id);const rect=canvas.getBoundingClientRect();canvas.width=rect.width*devicePixelRatio;canvas.height=rect.height*devicePixelRatio;ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);ctx.lineCap='round';ctx.lineJoin='round';if(data){let img=new Image();img.onload=()=>ctx.drawImage(img,0,0,rect.width,rect.height);img.src=data}}canvas._resize=resize;resize();let drawing=false,last=null;function pos(e){const r=canvas.getBoundingClientRect();return{x:e.clientX-r.left,y:e.clientY-r.top}}function start(e){e.preventDefault();drawing=true;last=pos(e)}function move(e){if(!drawing)return;e.preventDefault();let p=pos(e);ctx.strokeStyle=drawColor;ctx.lineWidth=drawSize;ctx.beginPath();ctx.moveTo(last.x,last.y);ctx.lineTo(p.x,p.y);ctx.stroke();last=p;localStorage.setItem(id,canvas.toDataURL())}function end(){drawing=false;localStorage.setItem(id,canvas.toDataURL())}canvas.addEventListener('pointerdown',start);canvas.addEventListener('pointermove',move);canvas.addEventListener('pointerup',end);canvas.addEventListener('pointerleave',end)}function resizeCanvases(){['journalCanvas','colorCanvas'].forEach(id=>{let c=document.getElementById(id);if(c&&c._resize)c._resize()})}function clearCanvas(id){const c=document.getElementById(id);if(!c)return;c.getContext('2d').clearRect(0,0,c.width,c.height);localStorage.removeItem(id)}setupCanvas('journalCanvas');setupCanvas('colorCanvas');window.addEventListener('resize',resizeCanvases);document.querySelectorAll('textarea,input:not([type])').forEach(e=>{if(!e.id)return;e.value=localStorage.getItem(e.id)||'';e.addEventListener('input',()=>localStorage.setItem(e.id,e.value))});document.querySelectorAll('input[type=checkbox]').forEach(e=>{let k=e.dataset.key;if(!k)return;e.checked=localStorage.getItem(k)==='true';e.addEventListener('change',()=>localStorage.setItem(k,e.checked))});